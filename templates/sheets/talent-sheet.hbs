<form class="{{cssClass}} flexcol" autocomplete="off">
  {{! Item Sheet Header }}
  <div>
    <div class="dl-border w-fc h-100">
      <img
        class="w-100p border-none br-5"
        src="{{item.img}}"
        data-edit="img"
        title="{{item.name}}"
      />
    </div>

    <div class="w-100 padding-sm">
      <div class="flex-col flex-wrap magin-b-sm">
        <h1 class="center">{{localize "twdu.talent"}}</h1>
        <br />
      </div>
      <div class="grid grid-2col-fr center gap-sm h-100">
        <div class="grid-span-2 grid-align-end">
          <b>{{localize "twdu.name"}}:</b>
          <span>
            <input
              class="light"
              name="name"
              type="text"
              value="{{item.name}}"
              style="width: 99%;"
            />
          </span>
        </div>

        <div class="grid-span-2">
          <b>{{localize "twdu.description"}}</b>
          <textarea
            name="system.description"
          >{{data.system.description}}</textarea>
        </div>
      </div>
    </div>
  </div>
</form>